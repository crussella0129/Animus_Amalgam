# Agent Tasks (Persistent Backlog)

- [ ] T-106 (backlog) [intent: INT-0001]: Write a follow-on intent rather than rewriting INT-0001 once it is realized. It sets the upstream sync policy: how NousResearch/hermes-agent changes enter main, and how main returns to dev through sync-work-branch.sh. It covers conflict handling for docs/ and the .gitignore sprint-loops block, which inherited CI checks must pass on dev-to-main checkpoints, content screening before public pushes (Sprint 0 plan critique C-002), and a reusable Book test harness with negative (mutation) cases that CI runs on checkpoints (Sprint 0 test critique C-002) — touches: docs/intents/, docs/intents/README.md, docs/SUMMARY.md
- [ ] T-210 (backlog) [intent: INT-0007]: Prerequisite: T-211's live replay of the post-attempt-13 lab changes (the launch-time dirty-tree refusal and the Hermes served-window check), with `agent.environment_probe: false` set (L-19). Keep a 20–30-turn local session fast on the existing lab. Use a longer multi-file task that accumulates real tool results, at a larger fixed context priced by the estimator. Compare thinking off / bounded / on, next-token speculative decoding on / off, and default / denser checkpoint spacing. Record per-turn uncached prompt and decoded tokens, decode rate, wall time, independent completion and resources. Enable compression with deterministic pruning first. First pass the missing `SYSTEMDRIVE` through the lab child environment, and approve the envelope once rather than per attempt (L-18) — touches: evals/local_qualification/, future sprint plan, INT-0007 evidence
- [ ] T-211 (backlog) [intent: INT-0004]: Harden the operational lab before paired trials. This is the Sprint 2 test-critique carry-forward ([critique-01](../sprints/s2/sprint-tests/critique-01.md) C-003 to C-006, C-008, C-009; [critique-02](../sprints/s2/sprint-tests/critique-02.md) C-016, C-019). Extract and test the stale-telemetry, scheduler-lag, launch-count and aggregate-time predicates. Add a stalled-collector and a deadline-triggered cleanup check that asserts ≤ 5 s and a surviving sentinel. Decide whether to require a server-level output limit: llama-server reported `n_predict = -1` despite `--predict 128`, so only the request cap binds. Observe backend truncation at the cap. Record the backend launch configuration, the host condition and the measured rendered-prefix size in the frozen manifest. Add an A→B→A temporary-home check for the lab wire and Hermes route. Set `agent.environment_probe: false` and prove byte-identical cross-session smoke prompts (L-19). Use a fresh home and fixture per attempt, or record the shared state. Own the live replay of every post-attempt-13 lab change (extractions, launch-time dirty refusal) on the next lab launch, before T-210 — touches: evals/local_qualification/, tests/evals/, future sprint plan
- [ ] T-202 (backlog) [intent: INT-0004]: Run paired native/static constrained qualification and the separate context-policy trials under the accepted bounded protocol; publish all results and decide whether static constraints advance — touches: future sprint plan, experiment receipts, INT-0004 evidence
- [ ] T-203 (backlog) [intent: INT-0005]: After INT-0004 qualifies a control, implement the smallest deterministic adaptive-policy boundary that satisfies the provider/cache/authority contract, using a plugin or Rust service only for a demonstrated need — touches: future sprint plan, standalone plugin/service or existing provider seam
- [ ] T-204 (backlog) [intent: INT-0005]: Run paired static/adaptive qualification, cache/profile isolation and cancellation checks; decide whether adaptation and any provider/service component advance — touches: future sprint plan, experiment receipts, INT-0005 evidence
- [ ] T-205 (backlog) [intent: INT-0006]: Only after a reproducible L-14 engine gap, compare existing constraint engines and a bounded algorithm prototype; otherwise record that the activation gate was not met — touches: future sprint plan, engine benchmark receipts, INT-0006 evidence
